services:
  - type: web
    name: campus-delivery-bot
    env: python
    buildCommand: pip install --no-cache-dir -r requirements.txt
    startCommand: python bot.py
    envVars:
      - key: PORT
        value: 10000